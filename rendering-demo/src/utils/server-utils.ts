import "server-only";

export const ServerSideFunction = () => {
  console.log(
    `use multiple libraries, interact with a database, process confidential information`
  );

  return "srever result";
};
